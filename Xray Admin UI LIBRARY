--[=[
 d888b  db    db d888888b      .d888b.      db      db    db  .d8b.  
88' Y8b 88    88   `88'        VP  `8D      88      88    88 d8' `8b 
88      88    88    88            odD'      88      88    88 88ooo88 
88  ooo 88    88    88          .88'        88      88    88 88~~~88 
88. ~8~ 88b  d88   .88.        j88.         88booo. 88b  d88 88   88    @uniquadev
 Y888P  ~Y8888P' Y888888P      888888D      Y88888P ~Y8888P' YP   YP  CONVERTER 

designed using localmaze gui creator
]=]

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

local Library = {}
local Commands = {}

-- Instances: 22 | Scripts: 2 | Modules: 0 | Tags: 0
local LMG2L = {};

-- Players.LocalPlayer.PlayerGui.XrayGui
LMG2L["XrayGui_1"] = Instance.new("ScreenGui", Players.LocalPlayer:WaitForChild("PlayerGui"));
LMG2L["XrayGui_1"]["Name"] = [[XrayGui]];
LMG2L["XrayGui_1"]["ZIndexBehavior"] = Enum.ZIndexBehavior.Sibling;
LMG2L["XrayGui_1"]["ResetOnSpawn"] = false;

-- Players.LocalPlayer.PlayerGui.XrayGui.Top
LMG2L["Top_2"] = Instance.new("Frame", LMG2L["XrayGui_1"]);
LMG2L["Top_2"]["BorderSizePixel"] = 0;
LMG2L["Top_2"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
LMG2L["Top_2"]["Size"] = UDim2.new(0, 286, 0, 46);
LMG2L["Top_2"]["Position"] = UDim2.new(0, 488, 0, 50);
LMG2L["Top_2"]["Name"] = [[Top]];
LMG2L["Top_2"]["AnchorPoint"] = Vector2.new(0, 0);

-- Players.LocalPlayer.PlayerGui.XrayGui.Top.UICorner
LMG2L["UICorner_3"] = Instance.new("UICorner", LMG2L["Top_2"]);

-- Players.LocalPlayer.PlayerGui.XrayGui.Top.UIStroke
LMG2L["UIStroke_4"] = Instance.new("UIStroke", LMG2L["Top_2"]);
LMG2L["UIStroke_4"]["Color"] = Color3.fromRGB(131, 131, 131);

-- Players.LocalPlayer.PlayerGui.XrayGui.Top.Thumbnail
LMG2L["Thumbnail_5"] = Instance.new("ImageLabel", LMG2L["Top_2"]);
LMG2L["Thumbnail_5"]["BorderSizePixel"] = 0;
LMG2L["Thumbnail_5"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
LMG2L["Thumbnail_5"]["Image"] = [[rbxasset://textures/ui/GuiImagePlaceholder.png]];
LMG2L["Thumbnail_5"]["Size"] = UDim2.new(0, 34, 0, 34);
LMG2L["Thumbnail_5"]["Name"] = [[Thumbnail]];
LMG2L["Thumbnail_5"]["Position"] = UDim2.new(0, 6, 0, 6);

-- Players.LocalPlayer.PlayerGui.XrayGui.Top.Thumbnail.UICorner
LMG2L["UICorner_6"] = Instance.new("UICorner", LMG2L["Thumbnail_5"]);
LMG2L["UICorner_6"]["CornerRadius"] = UDim.new(1, 0);

-- Players.LocalPlayer.PlayerGui.XrayGui.Top.Name
LMG2L["Name_8"] = Instance.new("TextLabel", LMG2L["Top_2"]);
LMG2L["Name_8"]["TextWrapped"] = true;
LMG2L["Name_8"]["BorderSizePixel"] = 0;
LMG2L["Name_8"]["TextXAlignment"] = Enum.TextXAlignment.Left;
LMG2L["Name_8"]["TextScaled"] = true;
LMG2L["Name_8"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
LMG2L["Name_8"]["FontFace"] = Font.new([[rbxasset://fonts/families/GothamSSm.json]], Enum.FontWeight.Medium, Enum.FontStyle.Normal);
LMG2L["Name_8"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
LMG2L["Name_8"]["BackgroundTransparency"] = 1;
LMG2L["Name_8"]["Size"] = UDim2.new(0, 78, 0, 20);
LMG2L["Name_8"]["Text"] = [[Welcome Daniel ]];
LMG2L["Name_8"]["Name"] = [[Name]];
LMG2L["Name_8"]["Position"] = UDim2.new(0, 46, 0, 14);

-- Players.LocalPlayer.PlayerGui.XrayGui.Main
LMG2L["Main_a"] = Instance.new("Frame", LMG2L["XrayGui_1"]);
LMG2L["Main_a"]["BorderSizePixel"] = 0;
LMG2L["Main_a"]["BackgroundColor3"] = Color3.fromRGB(0, 0, 0);
LMG2L["Main_a"]["Size"] = UDim2.new(0, 286, 0, 346);
LMG2L["Main_a"]["Position"] = UDim2.new(0, 488, 0, 100);
LMG2L["Main_a"]["Name"] = [[Main]];
LMG2L["Main_a"]["AnchorPoint"] = Vector2.new(0, 0);

-- Players.LocalPlayer.PlayerGui.XrayGui.Main.UICorner
LMG2L["UICorner_b"] = Instance.new("UICorner", LMG2L["Main_a"]);

-- Players.LocalPlayer.PlayerGui.XrayGui.Main.UIStroke
LMG2L["UIStroke_c"] = Instance.new("UIStroke", LMG2L["Main_a"]);
LMG2L["UIStroke_c"]["Color"] = Color3.fromRGB(131, 131, 131);

-- Players.LocalPlayer.PlayerGui.XrayGui.Main.CommandBar
LMG2L["CommandBar_d"] = Instance.new("TextBox", LMG2L["Main_a"]);
LMG2L["CommandBar_d"]["Name"] = [[CommandBar]];
LMG2L["CommandBar_d"]["PlaceholderColor3"] = Color3.fromRGB(209, 209, 209);
LMG2L["CommandBar_d"]["BorderSizePixel"] = 0;
LMG2L["CommandBar_d"]["TextColor3"] = Color3.fromRGB(255, 255, 255);
LMG2L["CommandBar_d"]["BackgroundColor3"] = Color3.fromRGB(255, 255, 255);
LMG2L["CommandBar_d"]["PlaceholderText"] = [[Command Bar...]];
LMG2L["CommandBar_d"]["Size"] = UDim2.new(0, 278, 0, 18);
LMG2L["CommandBar_d"]["Position"] = UDim2.new(0, 4, 0.02, -2);
LMG2L["CommandBar_d"]["Text"] = [[]];
LMG2L["CommandBar_d"]["BackgroundTransparency"] = 1;
LMG2L["CommandBar_d"]["ClearTextOnFocus"] = false;

-- Players.LocalPlayer.PlayerGui.XrayGui.Main.CommandBar.UIStroke
LMG2L["UIStroke_e"] = Instance.new("UIStroke", LMG2L["CommandBar_d"]);
LMG2L["UIStroke_e"]["Transparency"] = 0.6;
LMG2L["UIStroke_e"]["ApplyStrokeMode"] = Enum.ApplyStrokeMode.Border;
LMG2L["UIStroke_e"]["Color"] = Color3.fromRGB(131, 131, 131);

-- Players.LocalPlayer.PlayerGui.XrayGui.Main.CommandBar.UICorner
LMG2L["UICorner_f"] = Instance.new("UICorner", LMG2L["CommandBar_d"]);
LMG2L["UICorner_f"]["CornerRadius"] = UDim.new(0, 5);

-- Container de comandos que pode virar ScrollingFrame
local cmdContainer = Instance.new("Frame")
cmdContainer.Name = "CommandsContainer"
cmdContainer.BorderSizePixel = 0
cmdContainer.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
cmdContainer.BackgroundTransparency = 1
cmdContainer.Size = UDim2.new(1, 0, 1, 0)
cmdContainer.Position = UDim2.new(0, 0, 0, 28)
cmdContainer.Parent = LMG2L["Main_a"]

-- ScrollingFrame de backup (escondido inicialmente)
local scrollFrame = Instance.new("ScrollingFrame")
scrollFrame.Name = "ScrollFrame"
scrollFrame.BorderSizePixel = 0
scrollFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
scrollFrame.BackgroundTransparency = 1
scrollFrame.Size = UDim2.new(1, 0, 1, -30)
scrollFrame.Position = UDim2.new(0, 0, 0, 28)
scrollFrame.ScrollBarThickness = 0 -- Barra invisível
scrollFrame.ScrollingDirection = Enum.ScrollingDirection.Y
scrollFrame.ElasticBehavior = Enum.ElasticBehavior.Never -- Sem efeito elástico
scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
scrollFrame.Visible = false
scrollFrame.Parent = LMG2L["Main_a"]

-- Template para comandos
local function createCommandTemplate(name)
    local cmdFrame = Instance.new("Frame")
    cmdFrame.Name = name
    cmdFrame.BorderSizePixel = 0
    cmdFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    cmdFrame.Size = UDim2.new(0, 282, 0, 50)
    cmdFrame.BackgroundTransparency = 1
    cmdFrame.Visible = true
    
    local title = Instance.new("TextLabel")
    title.Name = "Title"
    title.TextWrapped = true
    title.BorderSizePixel = 0
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.TextScaled = true
    title.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    title.FontFace = Font.new([[rbxasset://fonts/families/GothamSSm.json]], Enum.FontWeight.Medium, Enum.FontStyle.Normal)
    title.TextColor3 = Color3.fromRGB(255, 254, 254)
    title.BackgroundTransparency = 1
    title.Size = UDim2.new(0, 282, 0, 26)
    title.Text = ""
    title.Parent = cmdFrame
    
    local description = Instance.new("TextLabel")
    description.Name = "Description"
    description.TextWrapped = true
    description.BorderSizePixel = 0
    description.TextXAlignment = Enum.TextXAlignment.Left
    description.TextTransparency = 0.4
    description.TextScaled = true
    description.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    description.FontFace = Font.new([[rbxasset://fonts/families/GothamSSm.json]], Enum.FontWeight.Medium, Enum.FontStyle.Normal)
    description.TextColor3 = Color3.fromRGB(255, 254, 254)
    description.BackgroundTransparency = 1
    description.Size = UDim2.new(0, 262, 0, 22)
    description.Text = ""
    description.Position = UDim2.new(0, 0, 0, 26)
    description.Parent = cmdFrame
    
    return cmdFrame
end

-- Variável para controle
local commandCount = 0
local isUsingScroll = false

-- Função para verificar e alternar entre Frame e ScrollingFrame
local function checkScrollNeeded()
    local totalCommands = #cmdContainer:GetChildren()
    
    if totalCommands > 6 and not isUsingScroll then
        -- Mudar para ScrollingFrame
        isUsingScroll = true
        
        -- Mover todos os comandos para o ScrollingFrame
        for _, cmd in ipairs(cmdContainer:GetChildren()) do
            if cmd:IsA("Frame") then
                cmd.Parent = scrollFrame
            end
        end
        
        -- Atualizar CanvasSize
        scrollFrame.CanvasSize = UDim2.new(0, 0, 0, totalCommands * 54)
        
        -- Esconder container normal, mostrar scroll
        cmdContainer.Visible = false
        scrollFrame.Visible = true
        
    elseif totalCommands <= 6 and isUsingScroll then
        -- Voltar para Frame normal
        isUsingScroll = false
        
        -- Mover comandos de volta
        for _, cmd in ipairs(scrollFrame:GetChildren()) do
            if cmd:IsA("Frame") then
                cmd.Parent = cmdContainer
            end
        end
        
        scrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
        
        -- Mostrar container normal, esconder scroll
        cmdContainer.Visible = true
        scrollFrame.Visible = false
    end
end

-- Função para atualizar visibilidade dos comandos (pesquisa por Title E Description)
local function filterCommands(searchText)
    local visibleCount = 0
    local currentContainer = isUsingScroll and scrollFrame or cmdContainer
    
    for _, cmdFrame in ipairs(currentContainer:GetChildren()) do
        if cmdFrame:IsA("Frame") then
            local title = cmdFrame:FindFirstChild("Title")
            local description = cmdFrame:FindFirstChild("Description")
            
            if title and description then
                local searchLower = string.lower(searchText)
                local titleLower = string.lower(title.Text)
                local descLower = string.lower(description.Text)
                
                -- Pesquisa tanto no Title quanto na Description
                local foundInTitle = string.find(titleLower, searchLower, 1, true)
                local foundInDesc = string.find(descLower, searchLower, 1, true)
                
                if searchText == "" then
                    -- Se não tem texto, mostra todos
                    cmdFrame.Visible = true
                    cmdFrame.Position = UDim2.new(0, 2, 0, visibleCount * 54)
                    visibleCount = visibleCount + 1
                elseif foundInTitle or foundInDesc then
                    -- Se encontrou no Title OU Description
                    cmdFrame.Visible = true
                    cmdFrame.Position = UDim2.new(0, 2, 0, visibleCount * 54)
                    visibleCount = visibleCount + 1
                else
                    cmdFrame.Visible = false
                end
            end
        end
    end
    
    -- Atualizar CanvasSize se estiver usando scroll
    if isUsingScroll then
        scrollFrame.CanvasSize = UDim2.new(0, 0, 0, visibleCount * 54)
    end
end

-- Configurar eventos da CommandBar
LMG2L["CommandBar_d"]:GetPropertyChangedSignal("Text"):Connect(function()
    filterCommands(LMG2L["CommandBar_d"].Text)
end)

LMG2L["CommandBar_d"].FocusLost:Connect(function(enterPressed)
    if enterPressed then
        local commandText = LMG2L["CommandBar_d"].Text
        if commandText ~= "" then
            -- Executar comando
            local cmdData = Commands[string.lower(commandText)]
            if cmdData and cmdData.callback then
                local success, result = pcall(cmdData.callback)
                if not success then
                    warn("Erro ao executar comando '" .. commandText .. "': " .. tostring(result))
                end
            end
            LMG2L["CommandBar_d"].Text = ""
        end
    end
end)

-- Função para registrar comandos
function Library:RegisterCommand(name, description, callback)
    local cmdFrame = createCommandTemplate(name)
    cmdFrame:FindFirstChild("Title").Text = name
    cmdFrame:FindFirstChild("Description").Text = description
    cmdFrame.Visible = true
    
    -- Adicionar ao container atual
    if isUsingScroll then
        cmdFrame.Parent = scrollFrame
    else
        cmdFrame.Parent = cmdContainer
    end
    
    Commands[string.lower(name)] = {
        name = name,
        description = description,
        callback = callback
    }
    
    -- Verificar se precisa ativar scroll
    checkScrollNeeded()
    
    -- Reordenar comandos
    filterCommands(LMG2L["CommandBar_d"].Text)
    
    return cmdFrame
end

-- Função para carregar comandos via loadstring
function Library:LoadCommands(code)
    local success, err = pcall(function()
        local func = loadstring(code)
        if func then
            func(self)
        end
    end)
    
    if not success then
        warn("Erro ao carregar comandos: " .. tostring(err))
    end
end

-- Inicialização
local function init()
    local player = Players.LocalPlayer
    
    -- Configurar thumbnail
    local success, image = pcall(function()
        return Players:GetUserThumbnailAsync(
            player.UserId,
            Enum.ThumbnailType.HeadShot,
            Enum.ThumbnailSize.Size420x420
        )
    end)
    
    if success and image then
        LMG2L["Thumbnail_5"].Image = image
    end
    
    -- Configurar nome
    LMG2L["Name_8"].Text = "Welcome " .. player.DisplayName
    
    -- Animação de entrada do Top (vindo de cima)
    local topOriginalPos = LMG2L["Top_2"].Position
    LMG2L["Top_2"].Position = UDim2.new(topOriginalPos.X.Scale, topOriginalPos.X.Offset, topOriginalPos.Y.Scale, topOriginalPos.Y.Offset - 100)
    
    local tweenTop = TweenService:Create(
        LMG2L["Top_2"],
        TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        {Position = topOriginalPos}
    )
    tweenTop:Play()
    
    -- Animação de entrada do Main (vindo de baixo)
    local mainOriginalPos = LMG2L["Main_a"].Position
    LMG2L["Main_a"].Position = UDim2.new(mainOriginalPos.X.Scale, mainOriginalPos.X.Offset, mainOriginalPos.Y.Scale, mainOriginalPos.Y.Offset + 100)
    
    local tweenMain = TweenService:Create(
        LMG2L["Main_a"],
        TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        {Position = mainOriginalPos}
    )
    tweenMain:Play()
end

init()

-- Retornar a Library
Library.ScreenGui = LMG2L["XrayGui_1"]
Library.CommandBar = LMG2L["CommandBar_d"]

return Library, require
